plugins {
    id "eu.xenit.enterprise-conventions.oss" version "0.2.0"
}

rootProject.name = 'solr-backup'

include ':solr-backup'

include ':integration-tests'
["solr6"].each { version ->
    include ":integration-tests:${version}"
}
