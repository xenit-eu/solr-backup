<?xml version='1.0' encoding='UTF-8'?>
<solr>
    <str name="adminHandler">${adminHandler:org.alfresco.solr.AlfrescoCoreAdminHandler}</str>

    <backup>
        <repository name="swarm" class="eu.xenit.solr.backup.s3.swarm.SwarmS3BackupRepository" default="false">
            <str name="blob.s3.bucket.name">${BLOB_S3_BUCKET_NAME}</str>
            <str name="blob.s3.endpoint">${BLOB_S3_ENDPOINT}</str>
        </repository>
    </backup>
</solr>
