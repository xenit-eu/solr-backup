<?xml version='1.0' encoding='UTF-8'?>
<solr>
    <str name="adminHandler">${adminHandler:org.alfresco.solr.AlfrescoCoreAdminHandler}</str>
    <backup>
        <repository name="s3" class="eu.xenit.solr.backup.s3.S3BackupRepository" default="false">
            <str name="s3.bucket.name">${S3_BUCKET_NAME:}</str>

            <str name="s3.client.endpointOverride">${S3_CLIENT_ENDPOINT_OVERRIDE:http://s3.eu-central-1.amazonaws.com}</str>
            <str name="s3.client.region">${S3_CLIENT_REGION:eu-central-1}</str>

            <str name="s3.client.accessKey">${S3_CLIENT_ACCESS_KEY:}</str>
            <str name="s3.client.secretKey">${S3_CLIENT_SECRET_KEY:}</str>

            <str name="s3.proxy.host">${S3_PROXY_HOST:}</str>
            <int name="s3.proxy.port">${S3_PROXY_PORT:0}</int>

            <bool name="s3.client.forcePathStyle">${S3_CLIENT_FORCE_PATH_STYLE:false}</bool>
            <bool name="s3.client.checksumValidationEnabled">${S3_CLIENT_CHECKSUM_VALIDATION_ENABLED:true}</bool>
        </repository>
    </backup>
</solr>
